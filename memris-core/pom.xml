<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.thejuampi</groupId>
        <artifactId>memris-parent</artifactId>
        <version>0.2.4</version>
    </parent>

    <artifactId>memris</artifactId>
    <name>Memris</name>
    <description>Core in-memory storage engine with vectorized execution and indexes</description>

    <dependencies>
        <!-- Core (A-Z) -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>SparseBitSet</artifactId>
            <version>${sparsebitset.version}</version>
        </dependency>
        <dependency>
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy</artifactId>
            <version>${bytebuddy.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>
        <dependency>
            <groupId>org.roaringbitmap</groupId>
            <artifactId>RoaringBitmap</artifactId>
            <version>${roaringbitmap.version}</version>
        </dependency>

        <!-- Tests (A-Z) -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>${jmh.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <version>${jmh.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>add-jmh-test-sources</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>add-test-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>src/jmh/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>-Xms256m -Xmx768m --enable-native-access=ALL-UNNAMED</argLine>
                    <excludedGroups>benchmark</excludedGroups>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>1.19.1</version>
                <configuration>
                    <skip>true</skip>
                    <targetClasses>
                        <param>io.memris.index.*</param>
                        <param>io.memris.kernel.RowIdSetFactory</param>
                        <param>io.memris.kernel.RowIdBitSet</param>
                        <param>io.memris.kernel.RowIdArraySet</param>
                        <param>io.memris.storage.heap.PageColumnString</param>
                        <param>io.memris.storage.heap.PageColumnInt</param>
                        <param>io.memris.storage.heap.PageColumnLong</param>
                        <param>io.memris.storage.SelectionImpl</param>
                    </targetClasses>
                    <targetTests>
                        <param>io.memris.index.*</param>
                        <param>io.memris.kernel.*</param>
                        <param>io.memris.storage.*</param>
                        <param>io.memris.storage.heap.*</param>
                    </targetTests>
                    <mutationThreshold>85</mutationThreshold>
                    <coverageThreshold>90</coverageThreshold>
                    <jvmArgs>
                        <arg>-Xms2g</arg>
                        <arg>-Xmx4g</arg>
                        <arg>-XX:+UseG1GC</arg>
                    </jvmArgs>
                    <threads>8</threads>
                    <timeoutFactor>20</timeoutFactor>
                    <timeoutConstant>30000</timeoutConstant>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-junit5-plugin</artifactId>
                        <version>1.2.3</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>pw.krejci</groupId>
                <artifactId>jmh-maven-plugin</artifactId>
                <version>${jmh.plugin.version}</version>
                <executions>
                    <execution>
                        <id>run-benchmarks</id>
                        <goals>
                            <goal>benchmark</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <warmupIterations>2</warmupIterations>
                    <measurementIterations>3</measurementIterations>
                    <forks>1</forks>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
