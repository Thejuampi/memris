# Plan: Fix ManyToMany Metadata NPE

## Goal
Fix the @ManyToMany metadata NPE by avoiding null/blank column names in the column lookup map, while preserving existing join resolution behavior.

## Plan
### 1. Update column map construction
- File: `memris-core/src/main/java/io/memris/core/EntityMetadata.java`
- Change the column name lookup map builder to skip entries where `FieldMapping.columnName()` is null or blank.
- Keep the property-to-position map unchanged so property-based lookups still work for relationship fields.

### 2. Keep ManyToMany mapping semantics unchanged
- File: `memris-core/src/main/java/io/memris/core/MetadataExtractor.java`
- No changes needed; inverse-side @ManyToMany can keep null column names as a signal.

## Verification
- Run the focused ManyToMany test:
```bash
mvn.cmd -q -e -pl memris-core test -Dtest=ECommerceRealWorldTest#shouldHydrateManyToManyJoinCollections
```
- If it passes, consider running full memris-core tests:
```bash
mvn.cmd -q -e -pl memris-core test
```

## Success Criteria
- ManyToMany test passes without NPE.
- No regression in other memris-core tests.
